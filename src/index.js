const { firstImplementation } = require('./first');
const { secondImplementation } = require('./second');
const { thirdImplementation } = require('./third');
const { fourthImplementation } = require('./fourth');

module.exports = {
  firstImplementation,
  secondImplementation,
  thirdImplementation,
  fourthImplementation,
};
